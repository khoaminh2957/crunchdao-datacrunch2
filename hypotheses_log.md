# Competition Hypotheses Log

> **How to update**: This is a living document. After each cloud run, append the run ID, sub number, and key metrics (Spearman, leaderboard delta) under the relevant hypothesis's **Evidence** column. When evidence is conclusive, flip **Status** to VERIFIED or FALSIFIED and update **Next test**. Keep entries chronological within each Evidence cell (newest at bottom).

**Status legend**: UNVERIFIED (no data yet) | PENDING (run submitted, awaiting result) | PARTIALLY VERIFIED (some evidence, not conclusive) | VERIFIED (confirmed) | FALSIFIED (disproven)

---

| ID | Statement | Rationale | Status | Evidence | Next test |
|----|-----------|-----------|--------|----------|-----------|
| H1 | Per-moon rank target boosts Spearman vs raw regression | Spearman is rank-based; training directly on per-moon ranks aligns loss with eval metric and removes cross-moon scale drift | PENDING | sub #5 will tell — awaiting cloud run result | Compare sub #5 (rank target) Spearman vs prior raw-regression baseline (sub #1–#4); if delta ≥ +0.005, mark VERIFIED |
| H2 | Recency weights tau=50 helps with distribution drift across 790+ moons | 790+ moons span long time range; exponential decay weight w=exp(-(M_max-M)/tau) up-weights recent moons where test distribution likely sits | PENDING | Awaiting first weighted-run submission | Submit identical pipeline with and without tau=50 weights; compare validation Spearman on held-out recent moons |
| H3 | Top-200 selected features beat using all 1150 features | 1150 features likely contain noise/redundancy; mutual-info or gain-based top-K reduces overfit and speeds training | PENDING | Awaiting feature-selection run | Run LGBM with all 1150 vs top-200 (selected by gain on train fold); compare CV Spearman and submission score |
| H4 | XGBoost matches or beats LGBM on this dataset | XGB and LGBM are usually within noise on tabular; worth a head-to-head to pick stronger base learner | UNVERIFIED | None yet | Train XGB with matched hyperparams (depth, lr, n_est) on same fold split; compare CV + sub Spearman |
| H5 | Ensemble (LGBM+XGB+CatBoost+Ridge) > any single model | Diverse model classes capture different signal; rank-average ensemble typically lifts Spearman 0.002–0.01 in tabular comps | UNVERIFIED | None yet | After H4 and CatBoost solo runs land, submit equal-weight rank-mean ensemble; require lift > best single by ≥ 0.003 |
| H6 | Neural MLP underperforms gradient boosting on tabular ~1150-feature data | Typical pattern in tabular comps (Kaggle, Numerai); GBDT dominates unless feature engineering is weak. Worth verifying to rule out | UNVERIFIED | None yet | Train 3-layer MLP (256-128-64, dropout 0.3) with same features and CV split; expect lower Spearman than LGBM baseline |
| H7 | DART boosting beats GBDT on this noisy regression task | DART's dropout-on-trees regularizes against noise; reported to help on noisy financial-style targets | UNVERIFIED | None yet | LGBM boosting_type=dart with matched n_estimators*1.5 (DART needs more trees); compare CV vs GBDT baseline |
| H8 | Cloud Python 3.10 vs local 3.14 — pandas/numpy/lgbm versions matter | Cloud env pins specific package versions; local Python 3.14 hit pandas compatibility issues. Version drift can break feature pipelines or change model behavior | PARTIALLY VERIFIED | pandas<2.3 required (confirmed by local env break); awaiting full version-pinning audit on cloud side | Lock requirements.txt to cloud-matching versions (pandas==2.2.x, numpy==1.26.x, lightgbm==4.x); run identical seed locally + cloud, compare outputs byte-for-byte |
